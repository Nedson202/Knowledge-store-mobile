import { StyleSheet } from 'react-native';
import { GEORGIA_BOLD, BLACK_2 } from '../settings';

const styles = StyleSheet.create({
  blackTextColor: {
    color: BLACK_2,
  },
  customText: {
    fontFamily: GEORGIA_BOLD,
    fontSize: 18,
    marginLeft: 15
  }
});

export default styles;
